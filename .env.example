
# Ambiente
# Opções: dev, test, production
NODE_ENV=dev

# Servidor
# Porta onde a aplicação irá rodar
PORT=3100

# Banco de Dados (PostgreSQL)
# URL de conexão com o PostgreSQL
# Formato: postgresql://usuario:senha@host:porta/database
DATABASE_URL=postgresql://docker:docker@localhost:5432/gympass

# Redis (Cache)
# Host do Redis
REDIS_HOST=127.0.0.1

# Porta do Redis
REDIS_PORT=6379

# JWT (Autenticação)
# Secret para assinar tokens JWT
# IMPORTANTE: Deve ter no mínimo 32 caracteres
# Use um gerador seguro ou: openssl rand -base64 32
JWT_SECRET=your-super-secret-jwt-key-minimum-32-characters-long

# Tempo de expiração do token JWT
# Formatos aceitos: 1h, 7d, 30d, etc.
JWT_EXPIRES_IN=7d


# CORS (Cross-Origin Resource Sharing)
# Origens permitidas (separar múltiplas por vírgula)
# Use * para permitir todas (apenas em dev)
# Em produção, liste apenas origens específicas
# Exemplo: http://localhost:3000,http://localhost:5173
CORS_ORIGINS=*
